<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Update your custom style to use Material Components styles -->
    <style name="MyDialogStyle" parent="Theme.MaterialComponents.Dialog">
        <!-- Use Material Components attributes for window appearance -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>

        <!-- Use Material Components attributes instead of deprecated ones -->
        <item name="android:windowActionBar">false</item>
        <item name="showTitle">false</item> <!-- Replaces windowNoTitle -->
    </style>

    <!-- Update NoActionBar style -->
    <style name="NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="showTitle">false</item> <!-- Replaces windowNoTitle -->
    </style>

</resources>
